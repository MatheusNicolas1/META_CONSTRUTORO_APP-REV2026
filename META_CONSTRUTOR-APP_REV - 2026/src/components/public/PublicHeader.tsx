import PublicNav from './PublicNav';

/**
 * PublicHeader — alias de PublicNav para consistência de nomenclatura.
 * O componente real que implementa a navegação está em PublicNav.tsx,
 * mas este barrel export permite importar como PublicHeader.
 */
export default PublicNav;
export { PublicNav };
